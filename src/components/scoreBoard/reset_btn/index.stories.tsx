import { Meta, StoryObj } from '@storybook/react';
import ResetBtn from '.';

export default {
  title: 'ScoreBoard/ResetBtn',
  component: ResetBtn,
} as Meta;

type Story = StoryObj<typeof ResetBtn>;

export const Primary: Story = {

};
